# API reference

> 📝 **Stub** — API pages will be auto-generated by Sphinx autodoc as
> docstrings are added to the codebase.

Planned API pages (one per top-level module):

- `downscaling.statistical` — lapse-rate + QDM + RBF pipeline
- `downscaling.dl` — U-Net FiLM training and inference
- `downscaling.prithvi` — Prithvi WxC backbone wrappers
- `downscaling.regimes` — synoptic regime classification
- `downscaling.io` — readers (CERRA, ERA5, Sencrop)
- `downscaling.metrics` — POD/FAR/CSI/RMSE/bias

Until then, please refer directly to the [source code on GitHub](https://github.com/maurinl26/downscaling/tree/main/downscaling)
and the `tests/` folder for usage patterns.
